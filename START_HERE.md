# 从这里开始

## 1. 启动系统

进入项目目录：

```bash
cd /Users/karwai/Documents/个人成长系统/read-level-system
```

启动本地系统：

```bash
python3 app.py
```

打开：

```text
http://127.0.0.1:8765
```

## 2. 去哪里导入资料？

打开页面：

```text
http://127.0.0.1:8765/import
```

你可以：

- 上传 PDF
- 上传 `.md` / `.txt` 公众号文章
- 直接粘贴文章正文

导入后，系统会自动进入这份资料的处理页。

## 3. 去哪里写？

打开：

```text
http://127.0.0.1:8765/materials
```

选择一份资料，进入它的处理页。

你会看到：

- 左边：原文预览
- 右边：结构化笔记
- 右边第二个页签：文章草稿

你真正写的地方在页面里，不需要自己点本地文件夹。

## 4. 首页什么时候会判断薄弱项？

不会一开始就判断。

规则是：

1. 没有导入资料：不判断薄弱项，四项能力都是 0。
2. 导入资料但没写笔记：不判断薄弱项，不加分。
3. 写了笔记但没评分：显示“等待评分”，不自动加分。
4. 生成真实 `score_growth.json` 后，首页才会展示 AI 薄弱项和能力变化。

## 5. demo 在哪里？

示例只在：

```text
examples/今日简史_demo/
```

它不会进入真实首页，也不会给你的能力加分。

